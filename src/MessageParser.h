#pragma once

// bg:galaxy:start
// bg:galaxy:speed=1

/* What we need
 * Set a text color
 * Set a text font
 * Set a text position
 */

namespace MessageParser {
    void parse(const uint8_t *data, const size_t len, AsyncWebSocketClient *wsClient) {
        if (len == 0) return;
        
    }
};